# Codebase audit, 2026-09-23

Full-repository audit, one week after the 2026-09-16 audit. Six reviewers read
disjoint slices in parallel: the MCP server and Mission Control API, the capture and
sync functions, the AI pipeline and its prompts, the frontend data layer, the
frontend components and UX, and the migrations plus the Markdown docs. Each
reported only what it traced through the call path, and fixed it in place. Findings
that crossed a slice boundary were handed to the owning reviewer. Nothing in this
audit touched the live project; the three new migrations and the function changes
take effect only when applied and deployed.

## What was checked

`npx tsc -p tsconfig.app.json --noEmit` clean before and after. `npx eslint .`: 1
error before (`prefer-const` in `previewAuthStorage.ts`), 0 after.
`node scripts/check-brand-strings.mjs`: failing before (`CookieList.tsx`), passing
after. `node scripts/check-edge-functions.mjs`: passing (225 files).
`npm test`: 112 of 114 files pass. The two failing files, `EditorToolbar.test.tsx`
(1 test) and `ContactTopicsPanel.test.tsx` (7 tests), fail identically on an
unmodified checkout of the previous commit, so this audit did not cause them. The
topics tests pass one at a time; run together, one slow test times out and the
ones after it fail. The toolbar test takes about 40 s even alone. Both files passed
earlier the same day, so they depend on timing and machine load. They are listed
under open items.

## Most serious findings (all fixed)

- **MCP replies could reach the wrong account.** One `McpServer` and one transport
  served every request in an isolate, and replies were matched by JSON-RPC id,
  which every client numbers from 0 or 1. Two concurrent calls with the same id
  swapped results: one user received another user's tool output, and the other
  call hung. Reproduced locally with the deployed package versions. Each request
  now gets its own server and transport; tools are recorded once at startup.
  Build label `2026-09-23-server-per-request`.
- **Database functions readable across accounts.** `match_notes`, `match_media`,
  `match_note_chunks`, `match_claims` and `match_person_documents` run as owner,
  take the account id as an argument and never compared it with the caller.
  Several service-only functions (`deduct_ai_tokens`, `note_ai_input`,
  `cleanup_profile_duplicates`, the note-AI job functions, profile-audit helpers)
  were still executable by `anon` or `authenticated` because only one of PUBLIC,
  anon and authenticated had been revoked. Migration `20260923150000` adds the
  caller check, revokes all three and fails if any function is still open.
  `match_notes` now also honours `ai_visibility`.
- **Cross-account AI spend and writes.** `profile-audit` sweep and backfill ran
  paid audits on every account; `normalize-profile` apply and rollback trusted a
  `user_id` from a review-queue payload the user can write; `review-queue-bulk`
  reverts and `note-chat`'s `add_wikilink` had no owner check;
  `ai-moderate-content` had no authorization at all. All scoped to the caller.
- **Data deleted on a transient error.** `compute-connections` treated a failed
  `match_notes` call as "no matches" and deleted the note's connections;
  `normalizeText` in `profile-audit` stripped non-Latin letters, so different
  Chinese or Cyrillic values counted as duplicates and one was deleted;
  `profile-reconcile` could merge unrelated contacts for the same reason and on
  single first-name aliases.
- **Discord capture accepted any server.** `/capture` is a global command and
  nothing checked the guild; it now only works in the server saved in Settings.

## Other repairs by area

- **MCP and API:** tool errors are flagged `isError`; `get_action_items` queried a
  column that does not exist; `get_entity_context` never returned notes;
  `get_claims`, `search_images`, `get_note_media`, `get_connected_notes`,
  `find_path`, `get_stats`, `get_user_profile`, `get_group` and the collection
  tools now respect "hidden from AI" and trash; graph tools page past 1,000 rows;
  name lookups prefer an exact match and refuse ambiguous ones; `search_brain`
  paging no longer loops. A database error during key lookup now answers 503
  instead of "invalid key". `mc-api-*` stop answering 200 with zeros on errors and
  return 404 for deletes of rows the caller does not own.
- **Capture and sync:** AI failures in Slack and Slack-thread capture no longer
  lose the message; `quick-capture` saves without AI when credits are empty, so
  imports finish; Telegram pairing reports a failed save; GitHub export no longer
  crashes on non-ASCII metadata or overwrites edits made during export; vault
  import keeps note bodies intact; Drive retries failed files and records the
  import before the slow analysis step; `analyze-media` accepts the service key so
  Drive imports get OCR; email-change mails go to the right addresses;
  stopwords match whole words.
- **AI pipeline and prompts:** truncated model replies are flagged and never
  cached as results; note content, web results and MCP output are fenced as data
  and cannot trigger replacement edits; process-note gets today's date, validates
  dates, coerces field types and no longer auto-applies "review" verdicts or
  zero-confidence moments; prompt size and output caps added to the chat
  functions.
- **Frontend data layer:** failed saves, profile edits, relationship writes and
  group actions now show an error instead of failing silently; the AI-credits
  gate explains why nothing happened; out-of-order credit refreshes can no longer
  restore a spent balance; stale caches after edits are invalidated; realtime
  topic channels no longer die when a person is reopened quickly.
- **UI and UX:** the Slack setup page showed the wrong URL (`slack-capture`
  instead of `ingest-thought`); archived groups can be restored; drafts no longer
  carry over between groups or World entities; Settings tab links work from inside
  Settings and the avatar has a visible "Picture" tab; collection trees load past
  1,000 items; destructive actions use an in-app confirm dialog instead of
  `window.confirm`; about 57 icon-only buttons and 36 inputs got accessible
  names; hover-only controls are reachable by keyboard and touch.
- **Migrations and config:** `wiki_log` now accepts the operations `wiki-cleanup`
  writes (`20260923150200`); indexes on foreign keys that cascade on note delete
  (`20260923150100`); `config.toml` pins `verify_jwt` for all 90 functions, so a
  deploy cannot switch the gateway check on for `gdrive-webhook` or `link-note`.
- **Docs:** `TEST_SCENARIOS.md` matched against the UI (about 110 labels, routes
  and flows corrected); README, CONTRIBUTING, ARCHITECTURE, PROJECT_STRUCTURE,
  DATA_MODEL, CRON_JOBS and UX_BACKLOG brought in line with the code.

## Open items

- `EditorToolbar.test.tsx` and `ContactTopicsPanel.test.tsx` are timing-dependent
  (see above).
- `20260922051204` and `20260817150004` raise when the old token is missing, so a
  fresh database stops there; both are applied and cannot be edited.
- Commit `5fd31044` renamed `hub_*` to `godspeed_*` inside 19 applied migrations;
  whether a fresh build and live match exactly is unverified.
- `connected_apps` and `godspeed_api_usage` have no creating migration.
- `add_claim` still creates a contact on a non-exact name (product decision).
- Profile-extraction backfill queues work that skips unchanged notes.
- `generate_collection_schema` defaults to a deprecated Claude model, and
  `analyze-media` uses `pixtral-12b-2409`, which Mistral has deprecated; switching
  needs the router to stop sending `temperature` to newer Claude models.
- `QuickCapture`, `PremiumGate`, `SmartTagsPanel` and `FolderMappingSettings` are
  never rendered.
- Other members of the linked Discord server can still capture; the Telegram
  secret travels in the URL, not the `secret_token` header.

## Deployed 2026-09-23

- **Migrations** `20260923150000`, `20260923150100`, `20260923150200` applied to
  `tjeapelvjlmbxafsmjef` through the Management API's `/database/query`, each in
  one transaction, and recorded in `supabase_migrations.schema_migrations` by
  hand. First a dry run of all three in one transaction that was discarded:
  as `authenticated` for one account, inserting and editing a note worked,
  `match_notes` on the own account returned rows, and on another account raised
  "not authorized". Before applying, the live catalogue showed no policy, column
  default, view or invoker function using any function the migration closes.
  After applying: `anon` cannot execute `match_notes`, `note_ai_input`,
  `deduct_ai_tokens` or `cleanup_profile_duplicates`; `service_role` can;
  the `wiki_log` check accepts `rebuild_page`.
- **Edge functions:** all 90 deployed with
  `npx supabase@latest functions deploy --project-ref tjeapelvjlmbxafsmjef --use-api`.
  `config.toml` matched the live `verify_jwt` of every function beforehand
  (all false), and afterwards all 91 live functions are `ACTIVE`, none with
  `verify_jwt` on (the 91st, `generate-contact-profile-suggestions`, exists
  only live and was not touched).
- **Frontend** published through the Lovable project at commit `dc6d5255`; the
  `Settings` chunk served from menerio.com carries the `ingest-thought` Slack URL.
- **Smoke tests:** `GET /menerio-mcp` reports build
  `2026-09-23-server-per-request`; four concurrent `tools/call` requests all
  carrying JSON-RPC id 1 each got their own complete `get_stats` answer;
  `search_brain` returns claims and notes (the match functions work for the
  service role); `GET /mc-api-notes/search` answers 200.

## Second pass: the live system

Read-only pass over production the same evening (catalogue, `cron`, `net`,
job tables, edge and function logs for the last 24 h). Code fixes are in the
repository, not deployed; data repairs are proposed in
`docs/LIVE_REPAIRS_2026-09-23.sql` and were not run.

- **Anyone could mint an API key on any account.** The seven
  `godspeed_connect_*` functions and `godspeed_api_bump_usage` were executable
  by PUBLIC, anon and authenticated live, although `20260920120000` and
  `20260909120000` revoke them: `20260920120000` is not in the migration ledger
  and its revoke never ran live. `profile_dedup_sweep` and
  `profile_subset_label_sweep`, which exist only live, delete profile entries
  for any account id and were open to anon too; the public avatars policy let
  anon list account ids. Migration `20260923190000` closes all of them.
- **Note analysis answered 500 for every classified failure.** 385 of
  process-note's 406 500s in 24 h were an exhausted allowance (account
  4332607c, 13:33 to 20:47 UTC, refilled by hand), 13 a revoked lease. The job
  was already parked or rescheduled; the 500 made the worker file a second
  "uncertain" failure and close its slot. It now answers 402, 503, 409 or 422.
- **A remembered name decision outlived its contact.** The owner's "michael"
  pointed at a deleted contact with 790 recorded decisions; 566 notes link
  "Michael" to that dead id. Only live, unmerged contacts are recalled now.
- `connected_apps` does have a creating migration (`20260328154249`);
  `godspeed_api_usage` does not, and seven `profile_*` helper functions exist
  only live. 27 migrations from 20260816 to 20260920 were applied without being
  recorded, so `supabase db push` would re-run them.
- A Python client from a residential German address still calls the removed
  `hub-api-world` (6 times, last 19:26 UTC): a Mission Control checkout on
  another machine that predates the rename.

## Second pass: the other five lenses

A second round the same night, split by concern across the whole repository
instead of by folder: security, data integrity and concurrency, prompts and
agent experience, user journeys, performance and cost, plus the live-system
review above. Every finding below is new; none repeats the first pass.

**Security.** Embedded PDF, video and audio nodes and several "open source"
links accepted `javascript:` URLs, so a note body written by an API key, a
capture bot or the AI could run script in menerio.com when the owner opened it
(`src/lib/safe-url.ts` now gates them). The web-clip full-page view put its
content policy after the first `<head` text, so a crafted snapshot could run
unprotected. `media_analysis`, `note_chunks` and `note_connections` accepted
rows against another account's note id, which fed attacker text into that
owner's note chat as the note's own scan (`20260923161000`). User-configured MCP
servers were connected to without an address check (SSRF). The REST API
returned notes, contacts and action items hidden from AI (`_shared/mc-visibility.ts`).
Secret comparisons now use a digest compare; the sign-in `?redirect=` is checked.

**Data integrity.** Rolling back an older Lexicon revision overwrote later ones
and hand edits (`20260923170000` refuses a changed page). Offline sync
acknowledged edits to rows deleted elsewhere, retried a too-large note forever,
and its create path overwrote newer server rows. Undoing a profile clean-up
deleted the facts it should restore. Contact merges left moments, documents and
collection rows on the hidden contact (`20260923170100`). Normalization jobs
stopped for good after five runs (`20260923170300`). "Link to note" and
accepting a single moment suggestion always failed. Removed group members could
never be re-added. Telegram redeliveries created duplicate notes. Content
strikes are now counted atomically (`20260923170200`).

**Prompts and agent experience.** `get_claims` returned embedding vectors (about
2 MB per 100 facts) and could report another person's facts for a name; several
tools omitted the ids the next call needs; `search_entities` limited before
filtering; `create_entity` refused new names by substring; dates printed in US
format; group AI tools used drifted prompt copies without the data fence. An
empty system prompt in the settings table could replace the code's prompt with
nothing. The MCP server now carries top-level instructions. Output formats,
language rules and the "this is data" rule were fixed in thirteen prompts, and
the Connect AI docs and setup prompt were corrected.

**User journeys.** Autosave closed Markdown source mode and the next keystroke
saved over the Markdown edit. Browser Back left a note open. The dashboard's
daily connections cache was shared between accounts on one browser. About a
dozen links led nowhere, several failures showed as empty states, copy actions
reported success when the clipboard refused, and the Getting Started checklist
could not be completed. Menerio's name was hardcoded on screens the Cherishly
brand shows.

**Performance and cost.** Every page downloaded all ~1,500 icons before showing
anything; JS loaded up front fell from 2,028 kB to 1,065 kB (475 to 311 kB
gzip). The sidebar badge, command palette, credits hook, processing sweep and
note-status poll stopped over-fetching or polling hidden tabs. Several queries
stopped pulling embedding vectors. `compute-connections` writes in batches;
`wiki-ingest`, `github-pull`, `github-sync-export` and `get-graph-data` read
only what they use; `backfill-metadata` no longer re-embeds. Four duplicate or
unused indexes on `notes` are dropped (`20260923184500`).

### Applied during the second pass

- `20260923190000` (the `godspeed_connect_*` and profile sweep grants) applied
  with Michael's approval as soon as it was confirmed live: signed-out callers
  could create an API key on any account. Afterwards anon and authenticated
  cannot execute any of the eleven functions; `mc-api-notes` still answers 200.
- `20260923161000` was applied by mistake during a rehearsal: the file carries
  its own `BEGIN`/`COMMIT`, so the rehearsal's surrounding transaction did not
  contain it. Its live policies match the file exactly; it was then recorded in
  `schema_migrations`. The other five were rehearsed together inside a
  transaction that was rolled back: they move 1 moment and 2 participant links
  off merged contacts, drop 4 indexes and add 2 functions, and left the live
  database unchanged.

### Checks

`npx tsc -p tsconfig.app.json --noEmit` clean; `npx eslint .` 0 errors;
brand-string check passes; edge-function check passes (231 files). `npx vitest
run`: 1,143 passed, 8 failed, all 8 in the two timing-dependent files listed
under open items.

## Second pass deployed 2026-09-24

- **Migrations** `20260923170000`, `170100`, `170200`, `170300` and `184500`
  applied through the Management API, each in one transaction and recorded in
  `schema_migrations` (ten versions from this audit now recorded). After:
  `wiki_rollback_revision_for` and `record_content_strike` exist, no moment
  points at a merged contact, `notes` has 14 indexes (was 18).
- **Edge functions:** all 90 deployed; `verify_jwt` matched live for every
  function beforehand. `GET /menerio-mcp` reports build `2026-09-24-second-audit`.
- **Frontend** published through Lovable at `c9b13114`; the main chunk served
  from menerio.com is 489 kB (was 609 kB).
- **Data repairs** 1 to 4 of `LIVE_REPAIRS_2026-09-23.sql`, run after the
  function deploy so the old code could not re-create them. First rehearsed in
  a rolled-back transaction, then committed with the same counts: 566 notes had
  the dead "Michael" link removed from `metadata.matched_people`, 7 stale name
  decisions deleted, 3 notes moved from "processing" to "failed". The two
  `updated_at` triggers on `notes` were disabled inside that transaction and
  re-enabled before commit, so no note was re-stamped as recently edited; the
  `note_ai_note_changed` trigger ignores `metadata.matched_people`, so no AI
  job was queued. The two gdrive cron commands now pass
  `timeout_milliseconds := 15000`; the runs after the change answered 200/202.
  Repairs 5 (duplicate indexes on `godspeed_api_usage`) and 6 (the migration
  ledger) were not run.
- **Smoke tests:** three concurrent `tools/call` with the same JSON-RPC id each
  answered; `initialize` returns the new server instructions; `search_contacts`
  prints ids; `search_brain` answers; `mc-api-notes` and `mc-api-contacts`
  answer 200; a note hidden from AI answers 404 through `mc-api-notes/<id>`.
  The note AI queue is empty (1,131 completed, 12 old failures).
