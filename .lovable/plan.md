

## Update Favicon

### Change
- Copy `user-uploads://Menerio-Favicon-180.png` to `public/favicon.png` (overwriting the current non-square version)
- No changes to `index.html` needed — it already references `/favicon.png`

That's it — one file copy. The 180×180 square icon will replace the stretched wordmark.

